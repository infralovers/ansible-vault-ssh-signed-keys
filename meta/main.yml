galaxy_info:
  role_name: vault_ssh_auth
  namespace: infralovers
  author: infralovers
  description: Configure HashiCorp Vault suppport on SSH authentication backend for user key validation and host key validation
  company: Infralovers
  license: MIT

  galaxy_tags: [vault, ssh, security, userkey, hostkey, signedssh]

dependencies: []
